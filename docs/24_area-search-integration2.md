# エリア検索（ドリルダウンUX最適化）＆ 検索状態維持：実装計画書

## 1. 設計思想：シームレスな探索と検索状態の独立
- **状態の独立保持 (State Persistence)**: 「エリア」と「キーワード」を独立した検索軸として管理。キーワードがゼロになってもエリア選択（`area_id`）は破棄せず、広域検索として成立させる。
- **インクリメンタル・サーチの導入**: エリアを選択した瞬間（確定を待たず）に背景の店舗リストを更新し、ユーザーに即時フィードバックを返す。
- **暗黙的確定（Implicit Commit）**: 「決定」ボタンを押さずとも、枠外タップや階層の途中終了を「現在の選択肢での検索意思」とみなし、UXの摩擦を最小化する。
- **ステートフルな表示名**: 内部的な `area_id` と、ユーザーに提示する「表示名」を同期させ、ドリルダウン終了後も選択状態を可視化する。

---

## 2. 修正・影響ファイル一覧

| カテゴリ | 物理パス | 区分 | 内容 |
| :--- | :--- | :--- | :--- |
| **Logic** | `src/db/queries/searchQuery.ts` | 更新 | `area_id`（前方一致）とキーワード群の AND/OR 検索ロジックの整理。 |
| **UI** | `src/pages/header/HeaderSearch.tsx` | 更新 | キーワード削除（チップ削除）時、URLから `q` は消すが `area` パラメータを維持するよう `hx-get` を修正。 |
| **API** | `src/api/areaDrilldown.ts` | 更新 | クリック時に「次階層の取得」と「店舗リストの更新」を同時にトリガーするレスポンスの変更。 |
| **UI** | `src/components/SearchArea.tsx` | 更新 | 外側クリックでの「確定」処理および、JavaScriptによる表示名の一時書き換え。 |
| **Client** | `src/index.tsx` | 更新 | URLから `area` と `q` を個別に抽出し、TopPage を経由して各コンポーネントへ正しく分配する。 |

---

## 3. 実装ロードマップ（更新順序）

### Phase 1: キーワードとエリアの疎結合化
1. **キーワード削除ロジックの修正（HeaderSearch.tsx）**
   - キーワードチップの「×」ボタン押下時、現在のURLにある `area` パラメータを保持したままリクエストを投げるように変更。
   - すべてのキーワード（`q`）が空になっても、`area` パラメータが存在すればエリア絞り込み状態を維持して店舗リストを再描画する。
2. **ナビゲーションの定義**
   - ロゴ/トップ遷移：全パラメータ（area, q）を破棄し初期状態へ。
   - エリア選択：既存の `q` を維持したまま `area` を追加/更新。

### Phase 2: リアルタイム絞り込みの連動
1. **`api/areaDrilldown.ts` のレスポンス拡張**
   - ドリルダウン内のリスト項目に `hx-push-url="true"` を追加。
   - `HX-Trigger` ヘッダーを利用し、エリア選択の瞬間に店舗リスト（`#service-list`）を最新の `area` + `q` で再取得させる。

### Phase 3: 外側クリックによる「確定」と表示名の反映
1. **`SearchArea.tsx` の挙動修正**
   - 枠外タップ時、リロードではなく「現在選択されているIDでの検索確定」として振る舞うようスクリプトを調整。
   - JSを用いて、ボタンの「エリアを選択」テキストを選択された地域名（例：江戸川区）へ動的に書き換える。

---

## 4. 完了定義
- [x] `area_id` を用いた検索により、店舗リストが正しく表示される（修正完了）。
- [x] エリア選択モジュールの幅を 50% に制限し、レイアウトを最適化（修正完了）。
- [ ] **キーワードをすべて削除しても、選択済みのエリア絞り込みが解除されず維持される。**
- [ ] エリア選択時、既に入力されているキーワードとの「掛け合わせ検索」が正しく行われる。
- [ ] ドリルダウンの各階層をクリックした際、背後の店舗リストが自動でリフレッシュされる。
- [ ] 「東京都」を選んだ状態で枠外をタップすると、ドリルダウンが閉じ、東京都全体の結果が表示される。
- [ ] エリア選択完了後、ボタン内のテキストが選択したエリア名に更新される。